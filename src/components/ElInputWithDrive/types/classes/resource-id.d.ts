import DocumentObject from "../interfaces/document-object";

/**
 * {@link ResourceId} is a utility class for generating resource IDs for documents.
 */
class ResourceId{
  /**
   * Generate a resource ID for a document.
   */
  generate(document: DocumentObject): void
}

export default ResourceId
